import 'package:bot_dashboard/app/export_app.dart';

class HelperView extends StatelessWidget {
  const HelperView({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      child: Center(child: Text("Ayudas y soporte")),
    );
  }
}
